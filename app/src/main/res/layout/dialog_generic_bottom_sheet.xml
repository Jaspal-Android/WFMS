<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- ── Handle bar ── -->
        <ImageView
            android:id="@+id/icPullView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="@dimen/margin"
            android:background="@drawable/ic_pull" />

        <!-- ── Title — FIX: CustomRegularDarkTextView → theme color ── -->
        <TextView
            android:id="@+id/titleTextView"
            style="@style/CustomRegularTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="@dimen/margin"
            android:layout_marginTop="@dimen/margin_small"
            android:textColor="?attr/wfmsColorOnSurface"
            android:textStyle="bold"
            tools:text="Select Work Type" />

        <!-- ── Divider below title ── -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="@dimen/margin_small"
            android:alpha="0.1"
            android:background="?attr/wfmsColorOnSurface" />

        <!-- ── Search bar — FIX: raw EditText → TextInputLayout OutlinedBox ── -->
        <com.google.android.material.textfield.TextInputLayout
            style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="@dimen/margin"
            android:layout_marginTop="@dimen/margin_small"
            android:layout_marginBottom="@dimen/margin_small"
            android:hint="@string/search"
            app:boxStrokeColor="?attr/wfmsColorOutline"
            app:boxStrokeWidthFocused="2dp"
            app:hintTextColor="?attr/wfmsColorOnSurfaceVariant"
            app:startIconDrawable="@drawable/ic_search"
            app:startIconTint="?attr/wfmsColorOnSurfaceVariant">

            <com.google.android.material.textfield.TextInputEditText
                android:id="@+id/searchBar"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:inputType="text"
                android:padding="@dimen/padding_medium" />
        </com.google.android.material.textfield.TextInputLayout>

        <!-- ── RecyclerView — wrapped with maxHeight to prevent overflow ── -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:clipToPadding="false"
            android:maxHeight="360dp"
            android:paddingBottom="@dimen/padding"
            android:scrollbars="vertical" />

    </LinearLayout>
</layout>