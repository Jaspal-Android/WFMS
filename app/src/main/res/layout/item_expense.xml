<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="expense"
            type="com.atvantiq.wfms.models.reimbursement.detail.Expense" />
    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/margin_small_extra"
        app:cardBackgroundColor="?attr/wfmsColorSurfaceVariant"
        app:cardCornerRadius="@dimen/card_radius"
        app:cardElevation="0dp">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="@dimen/padding_small">

            <!-- Bullet dot -->
            <View
                android:id="@+id/bulletDot"
                android:layout_width="6dp"
                android:layout_height="6dp"
                android:background="@drawable/ic_circular_bg"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="@+id/tvExpenseType" />

            <TextView
                android:id="@+id/tvExpenseType"
                style="@style/CustomMediumTextView"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/margin_small"
                android:layout_marginEnd="@dimen/margin"
                android:text="@{expense.expenseType}"
                android:textColor="?attr/wfmsColorOnSurface"
                android:textStyle="bold"
                app:layout_constraintEnd_toStartOf="@id/tvAmountPerSite"
                app:layout_constraintStart_toEndOf="@id/bulletDot"
                app:layout_constraintTop_toTopOf="parent"
                tools:text="Travel" />

            <!-- Claimed amount below type -->
            <TextView
                android:id="@+id/tvClaimedAmount"
                style="@style/CustomSmallTextView"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/margin_small"
                android:layout_marginTop="@dimen/margin_2dp"
                android:text="@{@string/of_format(expense.claimedAmount)}"
                android:textColor="?attr/wfmsColorOnSurfaceVariant"
                app:layout_constraintEnd_toStartOf="@id/tvAmountPerSite"
                app:layout_constraintStart_toEndOf="@id/bulletDot"
                app:layout_constraintTop_toBottomOf="@id/tvExpenseType"
                tools:text="of ₹90.00" />

            <TextView
                android:id="@+id/tvAmountPerSite"
                style="@style/CustomMediumTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{@string/rupee_format(expense.amountPerSite)}"
                android:textColor="?attr/wfmsColorOnSurface"
                android:textStyle="bold"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                tools:text="₹30.00" />

        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.cardview.widget.CardView>
</layout>