<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="android.view.View" />
        <variable name="typeStatusInteger" type="Integer" />
        <variable name="assignedDate" type="String" />
        <variable name="showSelectableOption" type="Boolean" />
    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/margin_small"
        app:cardBackgroundColor="?attr/wfmsColorSurface"
        app:cardCornerRadius="@dimen/card_radius"
        app:cardElevation="@dimen/elevation_small">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingVertical="@dimen/padding_medium"
            android:paddingEnd="@dimen/padding">

            <!-- Checkbox — FIX: buttonTint for theme-aware color -->
            <com.google.android.material.checkbox.MaterialCheckBox
                android:id="@+id/cbWorkType"
                style="@style/CustomCheckBox"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:buttonTint="?attr/wfmsColorPrimary"
                android:visibility="@{showSelectableOption ? View.VISIBLE : View.GONE}"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <!-- Work Type Name — FIX: Dark style removed -->
            <TextView
                android:id="@+id/tvTypeName"
                style="@style/CustomRegularTextView"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/margin"
                android:layout_marginEnd="@dimen/margin_small"
                android:textColor="?attr/wfmsColorOnSurface"
                android:textStyle="bold"
                app:layout_constraintEnd_toStartOf="@id/tvTypeStatus"
                app:layout_constraintStart_toEndOf="@id/cbWorkType"
                app:layout_constraintTop_toTopOf="parent"
                tools:text="Network Outage" />

            <!-- Status chip — FIX: hardcoded green removed, binding adapter handles color -->
            <TextView
                android:id="@+id/tvTypeStatus"
                style="@style/CustomSmallTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@drawable/status_primary_bg"
                android:paddingHorizontal="@dimen/padding_small"
                android:paddingVertical="@dimen/padding_x_small"
                android:textStyle="bold"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                assignedSiteStatus="@{typeStatusInteger}"
                tools:text="OPEN"
                tools:textColor="@color/status_present_text" />

            <!-- Assigned date — FIX: added theme text color -->
            <TextView
                android:id="@+id/tvTypeId"
                style="@style/CustomSmallTextView"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/margin_small_extra"
                android:textColor="?attr/wfmsColorOnSurfaceVariant"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="@+id/tvTypeName"
                app:layout_constraintTop_toBottomOf="@id/tvTypeName"
                formatApiDateToMonthDayYear="@{assignedDate}"
                tools:text="12-12-2026" />

        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.cardview.widget.CardView>
</layout>